security.jwt.token.secret-key: my-secret-key

# security:
#   jwt:
#     token:
#       secret-key: my-secret-key

spring:
  datasource:
    url: jdbc:mysql://localhost/reactspringproductstore
    username: root
    password: root
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl:
        auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  # security:
  #   user:
  #     name: user
  #     password: user
  mail:
    host: smtp.gmail.com
    port: 587
    username: fazilordanuc@gmail.com
    password: rosfdkuccqzcalsf
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

logging:
  level:
    root: INFO
    org:
      springframework:
        security: TRACE
       
#  logging:
#    level:
#      org.hibernate.SQL: DEBUG
#      org.hibernate.type: TRACE
